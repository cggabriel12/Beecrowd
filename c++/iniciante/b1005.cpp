#include <bits/stdc++.h>

using namespace std;

int main() {

    double a = 0;
    double b = 0;

    cin >> a;
    cin >> b;

    cout << fixed << setprecision(5);
    cout << "MEDIA = " << (a * 3.5 + b * 7.5) / (3.5 + 7.5) << "\n";
    return 0;
}
