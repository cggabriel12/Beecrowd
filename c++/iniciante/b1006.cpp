#include <bits/stdc++.h>

using namespace std;

int main() {

    double a = 0;
    double b = 0;
    double c = 0;

    cin >> a >> b >> c;
    

    cout << fixed << setprecision(1);
    cout << "MEDIA = " << (a * 2 + b * 3 + c * 5) / (2 + 3 + 5) << "\n";
    return 0;
}
